spring:
  application:
    name: SpringCloud-Monitor-Admin
  cloud:
    nacos:
          discovery:
            server-addr: ${nacos.url}:8001
          config:
              server-addr: ${nacos.url}:8001
              group: DEFAULT_GROUP
              prefix: monitor
              file-extension: yaml